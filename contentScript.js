function scanText() {
  const text = document.body.innerText;
  console.log('Scanned Text:', text);
  // Add logic here to process the text as needed
}

// Call scanText when the script is injected
scanText();